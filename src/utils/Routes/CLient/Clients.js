import ListClientCatalog from "../../../components/Client/ListClientCatalog";

const route = [
    {
        path: "/client",
        exact: true,
        component: ListClientCatalog,
    },
];

export default route;